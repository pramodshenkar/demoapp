// import AppHeader from '@/components/AppHeader'

describe('AppHeader',() => {
        test('If user != LoggedIn => Dont show Logout button',() => {
            expect(true).toBe(true)
        })

        test('If user == LoggedIn => show Logout button',() => {
            expect(true).toBe(true)
        })
    })